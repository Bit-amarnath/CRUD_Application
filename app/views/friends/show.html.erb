<div class="p-2 mb-3 bg-light rounded-3">
  <div class="container-fluid py-2">
    <h3 class="display-4 fw-bold"><%= @friend.first_name%> <%= @friend.last_name%></h3>
    <p class="col-md-8 fs-4"><%= @friend.email %> | <%= @friend.phone%></p>
    <br>
    <br>
    <%= link_to "Edit", edit_friend_path(@friend), class:"btn btn-success btn-lg" %> |
    <%= link_to "Back", friends_path, class:"btn btn-secondary btn-lg" %>
  </div>
</div>
